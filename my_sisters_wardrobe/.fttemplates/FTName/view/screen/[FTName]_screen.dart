import 'package:flutter/material.dart';

class <FTName | pascalcase> extends StatelessWidget {
  const <FTName | pascalcase> ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(child: Text('<FTName | capitalcase> Structure'),),
    );
  }
}
